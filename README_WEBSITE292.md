NBA PER-75 Website292 — canonical historical headshot fallback build.

Start API and frontend exactly as prior builds. Headshots now prioritize the canonical historical B-Ref source map for records whose NBA CDN asset was proven to be the generic placeholder or was missing. The pack animation is untouched.
