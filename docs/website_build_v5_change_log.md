# V5 — Percentile Context + Category Fix

Season, Era, and Historical percentile contexts are now resolved explicitly from the percentile source. Category axes and the six category cards are recalculated from the selected context using the saved aggregation specification. The API also reports context availability. The underlying analytical files remain untouched.
