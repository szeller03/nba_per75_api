# Website72 install

1. Replace/extract this folder as a new sibling of Website71.
2. From the Website72 directory:
   npm install
   npm run dev
3. Keep the canonical FastAPI backend running on port 8000.
4. The frontend should call the backend through the Vite /api proxy.

This build does not alter the NBA data or recalculate SDI.
